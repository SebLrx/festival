// For format details, see https://aka.ms/devcontainer.json. For config options, see https://aka.ms/vscode-remote/devcontainer.json.
{
  "name": "XAMPP",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": { }
  },
  // Set *default* container specific settings.json values on container create.
  "settings": { },
  // Add the IDs of extensions you want installed when the container is created.
  "extensions": [ ],
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  // "forwardPorts": [],
  // Uncomment the next line to run commands after the container is created.
  // "postCreateCommand": "php -S localhost:8080 -t /workspace",
  // Uncomment the next line to run commands every time you start the container.
  // "postStartCommand": "echo 'Container was started'",
  // Uncomment the next line to run commands before the container is stopped.
  // "postStopCommand": "echo 'Container was stopped'",
  // Uncomment the next line to enable the Docker CLI inside the container. See https://aka.ms/vscode-remote/samples/docker-from-docker.
  // "mounts": [ "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind" ],
  "runArgs": [
    "-v", "${PWD}:/workspace",
    "-p", "80:80",
    "-p", "443:443"
  ]
}
